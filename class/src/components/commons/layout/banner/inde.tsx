import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 100px;
  background-color: yellow;
`;

export default function LayOutBanner() {
  return <Wrapper>여기는 베너영역입니다.</Wrapper>;
}
